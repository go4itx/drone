kind: pipeline
type: exec
name: backend

platform:
  os: darwin
  arch: amd64

clone:
  disable: true

steps:
- name: greeting
  commands:
  - echo hello backend, develop

---
kind: pipeline
type: exec
name: frontend

platform:
  os: darwin
  arch: amd64

clone:
  disable: true

steps:
- name: greeting
  commands:
  - echo hello frontend, develop

---
kind: pipeline
type: exec
name: final

platform:
  os: darwin
  arch: amd64

clone:
  disable: true

steps:
- name: final
  commands:
  - echo "The previous tasks have been completed, develop"

depends_on:
- backend
- frontend
